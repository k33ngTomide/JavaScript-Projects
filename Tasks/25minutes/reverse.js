
// const numberReverse = function(numbers){
  // for(let counter = 0; counter < numbers.length; counter++){
    // for(let newCounter = counter+1; newCounter < numbers.length; newCounter++){
      // if (numbers[counter] > numbers[newCounter]){
        // let temp = numbers[counter];
        // numbers[counter] = numbers[newCounter]
        // numbers[newCounter] = temp;
      // }
    // }
  // }
// 
  // return numbers;
// }

const numberReverse = function(numbers){
  
}

let input = [34, 56, 76, 84, 34, 23];
let answer = numberReverse(input);
console.log(answer);